# Define the name of the executable
bin_PROGRAMS = mycalci

mycalci_SOURCES = add.c sub.c mul.c div.c main.c

# Include directories (if needed)
AM_CPPFLAGS = -I.

# Compiler and linker flags (if needed)
# AM_CFLAGS = -Wall
# LDADD = -lm

# Optionally, you can define additional rules for your project
# For example, to install header files:
include_HEADERS = header.h
